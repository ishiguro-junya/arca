version: "2"

linters:
  default: standard
  enable:
    - gosec
    - modernize
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec

formatters:
  enable:
    - gofmt
